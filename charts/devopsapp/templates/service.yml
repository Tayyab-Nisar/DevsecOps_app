apiVersion: v1
kind: Service
metadata:
  name: {{.Values.appname}}
  namespace: {{.Values.appnamespace}}
spec:
  selector:
    app: {{.Values.appname}}
  ports:
    - port: {{.Values.Containerport}}
      targetPort: {{.Values.Serviceport}}
  type: {{.Values.appsvctyp}}